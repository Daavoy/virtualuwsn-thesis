# Data modelling

Basic data models based on the Pydantic framework

https://docs.pydantic.dev/latest/

```
python -m pip install -r requirements.txt
```

Setup `PYTHONPATH` to include the top-level folder:

```
export PYTHONPATH=<path to top-level folder>
```


# TODO - Requirements

- how to deal with traceability - how data has been transformed, or fused 
- how to deal with integrity 
- what should be added for data quality 
- multi-variate vs. multi-dimensional 
- access control in meta-data
- standard for positioning?
- UUID - for each observation
- use a standard for the allowable units (e.g., celcius)
- Use of standard for data types 
- Handling elevation since we are underwater....
- Decide what should be optional and what should not be optional